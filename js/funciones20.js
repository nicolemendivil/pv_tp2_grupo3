export function mostrarDatos(nombre, apellido,libreta){
    alert(`
        Datos ingresados:
         
        Nombre: ${nombre}
        Apellido:${apellido}
        Libreta Universitaria:${libreta}` 

    );
}